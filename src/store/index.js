import Vue from 'vue'
import Vuex from 'vuex'
import Register from './modul/register'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    register: Register
  }
})
